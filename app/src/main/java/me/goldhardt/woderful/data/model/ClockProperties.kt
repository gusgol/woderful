package me.goldhardt.woderful.data.model

object ClockProperties {

    object EMOM {
        const val CONFIG_ACTIVE_TIME_S = "config_active_time_s"
        const val CONFIG_ROUNDS = "config_rounds"
        const val CONFIG_REST_TIME = "config_rest_time"
    }
}